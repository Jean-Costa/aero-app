import React from 'react';
import ReactDOM from 'react-dom';
import { Inicial } from './inicial';

ReactDOM.render(
  <React.StrictMode>
    <Inicial nomeAplicacao/>
  </React.StrictMode>,
  document.getElementById('root')
);

